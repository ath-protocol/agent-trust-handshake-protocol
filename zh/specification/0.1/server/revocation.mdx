---
title: "令牌吊销"
sidebarTitle: "吊销"
description: "POST /ath/revoke — 吊销 ATH 访问令牌。"
---

## 令牌吊销

**`POST /ath/revoke`**

吊销一个 ATH 访问令牌，使其立即不可用。

## 请求

```json
{
  "client_id": "ath_travelbot_001",
  "client_secret": "ath_secret_xxxxx",
  "token": "ath_tk_xxxxxxxx"
}
```

### 请求字段

| 字段 | 类型 | 必填 | 描述 |
|-------|------|----------|-------------|
| `client_id` | string | 是 | 代理的客户端 ID |
| `client_secret` | string | 是 | 代理的客户端密钥（按照 [RFC 7009](https://datatracker.ietf.org/doc/html/rfc7009) 要求进行客户端认证） |
| `token` | string | 是 | 要吊销的 ATH 访问令牌 |

## 响应

成功吊销后返回 `200 OK`。

## 行为

- 实现者必须（MUST）验证 `client_id` 和 `client_secret` 匹配有效的代理注册
- 实现者必须（MUST）验证 `client_id` 与令牌绑定的 `agent_id` 对应的注册匹配。代理只能吊销自己的令牌
- 令牌在吊销后立即不可用
- 使用已吊销令牌的后续 API 调用必须（MUST）被拒绝
- 无论令牌是否有效或存在，吊销端点建议（SHOULD）返回 `200 OK`（按照 [RFC 7009 §2.2](https://datatracker.ietf.org/doc/html/rfc7009#section-2.2)，不泄露令牌存在信息）
- 代理、用户（通过管理界面）或管理员均可吊销令牌
- 吊销操作建议（SHOULD）记录审计日志
